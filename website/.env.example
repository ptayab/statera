# Create a form at https://formspree.io and paste the form id (the part after /f/).
# Server-side is preferred so the id is not exposed in the browser bundle.
FORMSPREE_FORM_ID=

# Kept for existing Vercel setups that already use the public name.
NEXT_PUBLIC_FORMSPREE_FORM_ID=
